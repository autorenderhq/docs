---
title: "Crop"
description: "Learn how to crop images using AutoRender crop modes, positions, and position strategies"
icon: "crop"
---

## Overview

AutoRender provides powerful cropping capabilities that allow you to extract specific regions from images, control how images fit target dimensions, and use intelligent positioning to find the best crop area. Cropping is essential when you need exact dimensions or want to focus on specific parts of an image.

### URL Structure

AutoRender URLs with cropping follow this structure:

```bash
https://assets.autorender.io/LOKVTtKVGb/w_600,h_600,c_crop/doc1/blackporsche.jpg
```

<img
  src="https://assets.autorender.io/LOKVTtKVGb/w_600,h_600,c_crop/doc1/blackporsche.jpg"
  alt="Cropped image"
  style={{ borderRadius:"0.5rem",maxWidth:"100%" }}
/>

## Crop Modes

AutoRender supports multiple crop modes that control how images are resized and cropped to fit target dimensions. Each mode has different behavior regarding aspect ratio preservation and content visibility.

Compare different crop modes:

<Tabs>
  <Tab title="Original image">
    ```bash
    https://assets.autorender.io/LOKVTtKVGb/doc1/blackporsche.jpg
    ```

    <img
      src="https://assets.autorender.io/LOKVTtKVGb/doc1/blackporsche.jpg"
      alt="Original image"
      style={{ borderRadius:"0.5rem",maxWidth:"100%" }}
    />
  </Tab>
  <Tab title="Crop (c_crop)">
    Crops the image to exact dimensions, centering the crop area by default. This mode extracts a specific region from the image.

    ```bash
    https://assets.autorender.io/LOKVTtKVGb/w_600,h_600,c_crop/doc1/blackporsche.jpg
    ```

    <img
      src="https://assets.autorender.io/LOKVTtKVGb/w_600,h_600,c_crop/doc1/blackporsche.jpg"
      alt="Cropped image"
      style={{ borderRadius:"0.5rem",maxWidth:"100%" }}
    />
  </Tab>
  <Tab title="Fill (c_fill)">
    Resizes the image to cover the entire target area, cropping if necessary to maintain aspect ratio. The entire target area is filled, but parts of the image may be cropped.

    ```bash
    https://assets.autorender.io/LOKVTtKVGb/w_600,h_600,c_fill/doc1/blackporsche.jpg
    ```

    <img
      src="https://assets.autorender.io/LOKVTtKVGb/w_600,h_600,c_fill/doc1/blackporsche.jpg"
      alt="Fill mode"
      style={{ borderRadius:"0.5rem",maxWidth:"100%" }}
    />
  </Tab>
  <Tab title="Fit (c_fit)">
    Resizes the image to fit within the target area, preserving the entire image. May add padding if aspect ratios don't match.

    ```bash
    https://assets.autorender.io/LOKVTtKVGb/w_600,h_600,c_fit/doc1/blackporsche.jpg
    ```

    <img
      src="https://assets.autorender.io/LOKVTtKVGb/w_600,h_600,c_fit/doc1/blackporsche.jpg"
      alt="Fit mode"
      style={{ borderRadius:"0.5rem",maxWidth:"100%" }}
    />
  </Tab>
</Tabs>

### Crop Mode (`c_crop`)

Crops the image to exact dimensions, centering the crop area by default. This mode extracts a specific region from the image.

**Use cases:**

- Profile pictures
- Square thumbnails
- Exact dimension requirements
- Extracting specific regions

### Fill Mode (`c_fill`)

Resizes the image to cover the entire target area, cropping if necessary to maintain aspect ratio. The entire target area is filled, but parts of the image may be cropped.

**Use cases:**

- Hero images
- Banner images
- Background images
- When you need exact dimensions with no padding

### Fit Mode (`c_fit`)

Resizes the image to fit within the target area, preserving the entire image. May add padding if aspect ratios don't match.

**Use cases:**

- Product images
- When you need to show the entire image
- Responsive images
- When padding is acceptable

## Crop Position

Control where the crop area is positioned when cropping images. Position determines which part of the image is included in the crop.

### Basic Position Values

Compare different crop positions:

<Tabs>
  <Tab title="Center (p_c)">
    Center crop - 600x600:

    ```bash
    https://assets.autorender.io/LOKVTtKVGb/w_600,h_600,c_crop,p_c/doc1/blackporsche.jpg
    ```

    <img
      src="https://assets.autorender.io/LOKVTtKVGb/w_600,h_600,c_crop,p_c/doc1/blackporsche.jpg"
      alt="Center crop"
      style={{ borderRadius:"0.5rem",maxWidth:"100%" }}
    />
  </Tab>
  <Tab title="Top-left (p_tl)">
    Top-left corner - 600x600:

    ```bash
    https://assets.autorender.io/LOKVTtKVGb/w_600,h_600,c_crop,p_tl/doc1/blackporsche.jpg
    ```

    <img
      src="https://assets.autorender.io/LOKVTtKVGb/w_600,h_600,c_crop,p_tl/doc1/blackporsche.jpg"
      alt="Top-left crop"
      style={{ borderRadius:"0.5rem",maxWidth:"100%" }}
    />
  </Tab>
  <Tab title="Top-right (p_tr)">
    Top-right corner:

    ```bash
    https://assets.autorender.io/LOKVTtKVGb/w_600,h_600,c_crop,p_tr/doc1/blackporsche.jpg
    ```

    <img
      src="https://assets.autorender.io/LOKVTtKVGb/w_600,h_600,c_crop,p_tr/doc1/blackporsche.jpg"
      alt="Top-right crop"
      style={{ borderRadius:"0.5rem",maxWidth:"100%" }}
    />
  </Tab>
  <Tab title="Bottom-left (p_bl)">
    Bottom-left corner:

    ```bash
    https://assets.autorender.io/LOKVTtKVGb/w_600,h_600,c_crop,p_bl/doc1/blackporsche.jpg
    ```

    <img
      src="https://assets.autorender.io/LOKVTtKVGb/w_600,h_600,c_crop,p_bl/doc1/blackporsche.jpg"
      alt="Bottom-left crop"
      style={{ borderRadius:"0.5rem",maxWidth:"100%" }}
    />
  </Tab>
  <Tab title="Bottom-right (p_br)">
    Bottom-right corner:

    ```bash
    https://assets.autorender.io/LOKVTtKVGb/w_600,h_600,c_crop,p_br/doc1/blackporsche.jpg
    ```

    <img
      src="https://assets.autorender.io/LOKVTtKVGb/w_600,h_600,c_crop,p_br/doc1/blackporsche.jpg"
      alt="Bottom-right crop"
      style={{ borderRadius:"0.5rem",maxWidth:"100%" }}
    />
  </Tab>
  <Tab title="Top (p_t)">
    Top center:

    ```bash
    https://assets.autorender.io/LOKVTtKVGb/w_600,h_600,c_crop,p_t/doc1/blackporsche.jpg
    ```

    <img
      src="https://assets.autorender.io/LOKVTtKVGb/w_600,h_600,c_crop,p_t/doc1/blackporsche.jpg"
      alt="Top center crop"
      style={{ borderRadius:"0.5rem",maxWidth:"100%" }}
    />
  </Tab>
  <Tab title="Bottom (p_b)">
    Bottom center:

    ```bash
    https://assets.autorender.io/LOKVTtKVGb/w_600,h_600,c_crop,p_b/doc1/blackporsche.jpg
    ```

    <img
      src="https://assets.autorender.io/LOKVTtKVGb/w_600,h_600,c_crop,p_b/doc1/blackporsche.jpg"
      alt="Bottom center crop"
      style={{ borderRadius:"0.5rem",maxWidth:"100%" }}
    />
  </Tab>
  <Tab title="Left (p_l)">
    Left center:

    ```bash
    https://assets.autorender.io/LOKVTtKVGb/w_600,h_600,c_crop,p_l/doc1/blackporsche.jpg
    ```

    <img
      src="https://assets.autorender.io/LOKVTtKVGb/w_600,h_600,c_crop,p_l/doc1/blackporsche.jpg"
      alt="Left center crop"
      style={{ borderRadius:"0.5rem",maxWidth:"100%" }}
    />
  </Tab>
  <Tab title="Right (p_r)">
    Right center:

    ```bash
    https://assets.autorender.io/LOKVTtKVGb/w_600,h_600,c_crop,p_r/doc1/blackporsche.jpg
    ```

    <img
      src="https://assets.autorender.io/LOKVTtKVGb/w_600,h_600,c_crop,p_r/doc1/blackporsche.jpg"
      alt="Right center crop"
      style={{ borderRadius:"0.5rem",maxWidth:"100%" }}
    />
  </Tab>
</Tabs>

### Position Reference

| Position Code | Description   | Full Name    |
| ------------- | ------------- | ------------ |
| `p_c`         | Center        | center       |
| `p_tl`        | Top-left      | top-left     |
| `p_tr`        | Top-right     | top-right    |
| `p_bl`        | Bottom-left   | bottom-left  |
| `p_br`        | Bottom-right  | bottom-right |
| `p_t`         | Top center    | top          |
| `p_b`         | Bottom center | bottom       |
| `p_l`         | Left center   | left         |
| `p_r`         | Right center  | right        |

## Advanced Position Strategies

AutoRender supports intelligent position detection that automatically finds the best crop area based on image content analysis.

Compare intelligent position strategies:

<Tabs>
  <Tab title="Original image">
    ```bash
    https://assets.autorender.io/LOKVTtKVGb/doc1/gpose.jpg
    ```

    <img
      src="https://assets.autorender.io/LOKVTtKVGb/doc1/blackporsche.jpg"
      alt="Original image"
      style={{ borderRadius:"0.5rem",maxWidth:"100%" }}
    />
  </Tab>
  <Tab title="Face Detection (p_face)">
    Automatically detects faces in the image and centers the crop on the detected face(s).

    ```bash
    https://assets.autorender.io/LOKVTtKVGb/w_600,h_600,c_crop,p_face/doc1/gpose.jpg
    ```

    <img
      src="https://assets.autorender.io/LOKVTtKVGb/w_600,h_600,c_crop,p_face/doc1/blackporsche.jpg"
      alt="Face detection crop"
      style={{ borderRadius:"0.5rem",maxWidth:"100%" }}
    />
  </Tab>
  <Tab title="Entropy (p_entropy)">
    Finds the most interesting or information-rich area of the image and centers the crop there.

    ```bash
    https://assets.autorender.io/LOKVTtKVGb/w_600,h_600,c_crop,p_entropy/doc1/gpose.jpg
    ```

    <img
      src="https://assets.autorender.io/LOKVTtKVGb/w_600,h_600,c_crop,p_entropy/doc1/blackporsche.jpg"
      alt="Entropy-based crop"
      style={{ borderRadius:"0.5rem",maxWidth:"100%" }}
    />
  </Tab>
  <Tab title="Attention (p_attention)">
    Finds the focal point or area that draws attention and centers the crop there.

    ```bash
    https://assets.autorender.io/LOKVTtKVGb/w_600,h_600,c_crop,p_attention/doc1/gpose.jpg
    ```

    <img
      src="https://assets.autorender.io/LOKVTtKVGb/w_600,h_600,c_crop,p_attention/doc1/blackporsche.jpg"
      alt="Attention-based crop"
      style={{ borderRadius:"0.5rem",maxWidth:"100%" }}
    />
  </Tab>
</Tabs>

### Face Detection (`p_face`)

Automatically detects faces in the image and centers the crop on the detected face(s).

**Use cases:**

- Profile pictures
- Portrait thumbnails
- Social media images
- When faces are the main subject

### Entropy-Based Positioning (`p_entropy`)

Finds the most interesting or information-rich area of the image and centers the crop there.

**Use cases:**

- Automatic thumbnail generation
- When you want the most visually interesting area
- Product images with varied content
- Landscape photography

### Attention-Based Positioning (`p_attention`)

Finds the focal point or area that draws attention and centers the crop there.

**Use cases:**

- Automatic image optimization
- When the subject location is unknown
- Complex compositions
- When manual positioning is not practical

<Info>
  Position strategies (`p_face`, `p_entropy`, `p_attention`) use advanced image analysis algorithms to automatically find the best crop area. These are particularly useful when you need to generate thumbnails or crops from images where the subject location varies.
</Info>

## Combining Crop with Aspect Ratio

You can combine crop modes with aspect ratios for precise control:

16:9 aspect ratio with specific width and face detection:

```bash
https://assets.autorender.io/LOKVTtKVGb/ar_16:9,w_1920,c_crop,p_face/doc1/blackporsche.jpg
```

<img
  src="https://assets.autorender.io/LOKVTtKVGb/ar_16:9,w_1920,c_crop,p_face/doc1/blackporsche.jpg"
  alt="Aspect ratio with crop"
  style={{ borderRadius:"0.5rem",maxWidth:"100%" }}
/>

## Best Practices

### Choosing the Right Crop Mode

- **`c_crop`**: When you need exact dimensions and can crop content
- **`c_fill`**: When you need to fill an area completely without padding
- **`c_fit`**: When you need to show the entire image

### Position Selection

- **Manual positions** (`p_tl`, `p_c`, etc.): When you know where the subject is
- **Face detection** (`p_face`): For portraits and people
- **Entropy** (`p_entropy`): For automatic thumbnail generation
- **Attention** (`p_attention`): For complex compositions

### Performance Considerations

- Position strategies (`p_face`, `p_entropy`, `p_attention`) require additional processing time
- Use manual positions when possible for faster processing
- Cache cropped images to avoid repeated processing

## Common Use Cases

### Thumbnails

Square thumbnail with face detection:

```bash
https://assets.autorender.io/LOKVTtKVGb/w_300,h_300,c_crop,p_face/doc1/blackporsche.jpg
```

### Hero Images

Full-width hero with fill mode:

```bash
https://assets.autorender.io/LOKVTtKVGb/w_1920,h_1080,c_fill,p_c/doc1/blackporsche.jpg
```

### Profile Pictures

Square profile with face detection:

```bash
https://assets.autorender.io/LOKVTtKVGb/w_400,h_400,c_crop,ar_1:1,p_face/doc1/blackporsche.jpg
```

### Product Images

Product image that fits within bounds:

```bash
https://assets.autorender.io/LOKVTtKVGb/w_800,h_800,c_fit/doc1/blackporsche.jpg
```

## Examples

### Example 1: Square Thumbnail

Create a 300x300 square thumbnail, cropping from center:

```bash
https://assets.autorender.io/LOKVTtKVGb/w_300,h_300,c_crop/doc1/blackporsche.jpg
```

<img
  src="https://assets.autorender.io/LOKVTtKVGb/w_300,h_300,c_crop/doc1/blackporsche.jpg"
  alt="Square thumbnail"
  style={{ borderRadius:"0.5rem",maxWidth:"100%" }}
/>

### Example 2: Banner Image

Create a 1920x600 banner, filling the area:

```bash
https://assets.autorender.io/LOKVTtKVGb/w_1920,h_600,c_fill,p_c/doc1/blackporsche.jpg
```

<img
  src="https://assets.autorender.io/LOKVTtKVGb/w_1920,h_600,c_fill,p_c/doc1/blackporsche.jpg"
  alt="Banner image"
  style={{ borderRadius:"0.5rem",maxWidth:"100%" }}
/>

### Example 3: Profile Picture with Face Detection

Create a square profile picture with automatic face detection:

```bash
https://assets.autorender.io/LOKVTtKVGb/w_400,h_400,c_crop,ar_1:1,p_face/doc1/blackporsche.jpg
```

<img
  src="https://assets.autorender.io/LOKVTtKVGb/w_400,h_400,c_crop,ar_1:1,p_face/doc1/blackporsche.jpg"
  alt="Profile picture with face detection"
  style={{ borderRadius:"0.5rem",maxWidth:"100%" }}
/>

### Example 4: Responsive Crop

Create a responsive image that fits within bounds:

```bash
https://assets.autorender.io/LOKVTtKVGb/w_800,c_fit/doc1/blackporsche.jpg
```

<img
  src="https://assets.autorender.io/LOKVTtKVGb/w_800,c_fit/doc1/blackporsche.jpg"
  alt="Responsive crop"
  style={{ borderRadius:"0.5rem",maxWidth:"100%" }}
/>