---
title: 'Rotate'
description: 'Learn how to rotate images using AutoRender rotation parameter'
icon: 'rotate'
---

## Overview

AutoRender's rotation feature allows you to rotate images by any angle in degrees. Rotation is useful for correcting image orientation, creating artistic effects, or adjusting images that were captured at different angles.

### URL Structure

AutoRender URLs with rotation follow this structure:

```bash
https://assets.autorender.io/LOKVTtKVGb/r_90/doc1/flight.jpg
```

<img src="https://assets.autorender.io/LOKVTtKVGb/r_90/doc1/flight.jpg" alt="Rotated image" style={{ borderRadius: '0.5rem', maxWidth: '100%' }} />

## How Rotation Works

Rotation rotates the image around its center point by the specified number of degrees. AutoRender normalizes rotation values to the 0-360 degree range, so you can use any value and it will be converted appropriately.

- **Positive values**: Rotate clockwise
- **Negative values**: Rotate counter-clockwise
- **Values > 360**: Automatically normalized to 0-360 range

## Common Rotations

Compare different rotation angles:

<Tabs>
<Tab title="Original (0°)">
```bash
https://assets.autorender.io/LOKVTtKVGb/doc1/flight.jpg
```

<img src="https://assets.autorender.io/LOKVTtKVGb/doc1/flight.jpg" alt="Original image" style={{ borderRadius: '0.5rem', maxWidth: '100%' }} />
</Tab>

<Tab title="90°">
Rotate the image 90 degrees clockwise:

```bash
https://assets.autorender.io/LOKVTtKVGb/r_90/doc1/flight.jpg
```

<img src="https://assets.autorender.io/LOKVTtKVGb/r_90/doc1/flight.jpg" alt="Rotated 90 degrees" style={{ borderRadius: '0.5rem', maxWidth: '100%' }} />
</Tab>

<Tab title="180°">
Rotate the image 180 degrees (upside down):

```bash
https://assets.autorender.io/LOKVTtKVGb/r_180/doc1/flight.jpg
```

<img src="https://assets.autorender.io/LOKVTtKVGb/r_180/doc1/flight.jpg" alt="Rotated 180 degrees" style={{ borderRadius: '0.5rem', maxWidth: '100%' }} />
</Tab>

<Tab title="270°">
Rotate the image 270 degrees clockwise (or 90 degrees counter-clockwise):

```bash
https://assets.autorender.io/LOKVTtKVGb/r_270/doc1/flight.jpg
```

<img src="https://assets.autorender.io/LOKVTtKVGb/r_270/doc1/flight.jpg" alt="Rotated 270 degrees" style={{ borderRadius: '0.5rem', maxWidth: '100%' }} />
</Tab>

<Tab title="45°">
Rotate the image 45 degrees:

```bash
https://assets.autorender.io/LOKVTtKVGb/r_45/doc1/flight.jpg
```

<img src="https://assets.autorender.io/LOKVTtKVGb/r_45/doc1/flight.jpg" alt="Rotated 45 degrees" style={{ borderRadius: '0.5rem', maxWidth: '100%' }} />
</Tab>
</Tabs>

## Arbitrary Angles

You can rotate images by any angle, not just 90-degree increments:

### 45 Degrees

```bash
https://assets.autorender.io/LOKVTtKVGb/r_45/doc1/flight.jpg
```

<img src="https://assets.autorender.io/LOKVTtKVGb/r_45/doc1/flight.jpg" alt="Rotated 45 degrees" style={{ borderRadius: '0.5rem', maxWidth: '100%' }} />

### 15 Degrees

```bash
https://assets.autorender.io/LOKVTtKVGb/r_15/doc1/flight.jpg
```

### 30 Degrees

```bash
https://assets.autorender.io/LOKVTtKVGb/r_30/doc1/flight.jpg
```

### Custom Angles

Rotate 23.5 degrees:

```bash
https://assets.autorender.io/LOKVTtKVGb/r_23.5/doc1/flight.jpg
```

Rotate 135 degrees:

```bash
https://assets.autorender.io/LOKVTtKVGb/r_135/doc1/flight.jpg
```

Rotate 315 degrees (same as -45 degrees):

```bash
https://assets.autorender.io/LOKVTtKVGb/r_315/doc1/flight.jpg
```

<Note>
  AutoRender automatically normalizes rotation values to the 0-360 degree range. For example, `r_450` becomes `r_90`, and `r_-45` becomes `r_315`.
</Note>

## Combining Rotation with Other Transformations

Rotation works well when combined with other transformations like resize, crop, and flip.

### Rotation with Resize

Rotate 90 degrees, then resize to 800px width:

```bash
https://assets.autorender.io/LOKVTtKVGb/r_90,w_800/doc1/flight.jpg
```

<img src="https://assets.autorender.io/LOKVTtKVGb/r_90,w_800/doc1/flight.jpg" alt="Rotated and resized" style={{ borderRadius: '0.5rem', maxWidth: '100%' }} />

### Rotation with Crop

Rotate 45 degrees, then crop to 600x600:

```bash
https://assets.autorender.io/LOKVTtKVGb/r_45,w_600,h_600,c_crop/doc1/flight.jpg
```

<img src="https://assets.autorender.io/LOKVTtKVGb/r_45,w_600,h_600,c_crop/doc1/flight.jpg" alt="Rotated and cropped" style={{ borderRadius: '0.5rem', maxWidth: '100%' }} />

### Rotation with Flip

Rotate 90 degrees and flip horizontally:

```bash
https://assets.autorender.io/LOKVTtKVGb/r_90,flip_h/doc1/flight.jpg
```

## Use Cases

### Correcting Image Orientation

Fix images that were captured in the wrong orientation:

Correct portrait orientation:

```bash
https://assets.autorender.io/LOKVTtKVGb/r_90/doc1/flight.jpg
```

### Creating Artistic Effects

Use rotation for creative effects:

Slight tilt for dynamic effect:

```bash
https://assets.autorender.io/LOKVTtKVGb/r_5/doc1/flight.jpg
```

Diagonal orientation:

```bash
https://assets.autorender.io/LOKVTtKVGb/r_45/doc1/flight.jpg
```

### Product Photography

Adjust product images for better presentation:

Rotate product for better viewing angle:

```bash
https://assets.autorender.io/LOKVTtKVGb/r_15,w_800/doc1/flight.jpg
```

## Best Practices

### Rotation Order

- Rotation is typically applied after resize and crop operations
- Consider the final dimensions when rotating, as rotation may change the effective image bounds
- For best results, rotate before applying effects or enhancements

### Performance

- Rotation processing is fast and efficient
- Rotation with transparent backgrounds preserves transparency
- Cached results improve performance for repeated requests

### Quality Considerations

- Rotation uses high-quality interpolation to maintain image quality
- Rotated images may have slightly different dimensions due to the rotation
- For exact dimensions, combine rotation with crop or resize

## Examples

### Example 1: Simple Rotation

Rotate image 90 degrees:

```bash
https://assets.autorender.io/LOKVTtKVGb/r_90/doc1/flight.jpg
```

<img src="https://assets.autorender.io/LOKVTtKVGb/r_90/doc1/flight.jpg" alt="Simple rotation" style={{ borderRadius: '0.5rem', maxWidth: '100%' }} />

### Example 2: Rotation with Resize

Rotate 45 degrees and resize to 800px width:

```bash
https://assets.autorender.io/LOKVTtKVGb/r_45,w_800/doc1/flight.jpg
```

<img src="https://assets.autorender.io/LOKVTtKVGb/r_45,w_800/doc1/flight.jpg" alt="Rotation with resize" style={{ borderRadius: '0.5rem', maxWidth: '100%' }} />

### Example 3: Rotation with Crop

Rotate 90 degrees, then crop to square:

```bash
https://assets.autorender.io/LOKVTtKVGb/r_90,w_600,h_600,c_crop/doc1/flight.jpg
```

<img src="https://assets.autorender.io/LOKVTtKVGb/r_90,w_600,h_600,c_crop/doc1/flight.jpg" alt="Rotation with crop" style={{ borderRadius: '0.5rem', maxWidth: '100%' }} />

### Example 4: Multiple Transformations

Rotate, resize, and apply border radius:

```bash
https://assets.autorender.io/LOKVTtKVGb/r_15,w_600,h_600,c_crop,br_20/doc1/flight.jpg
```

<img src="https://assets.autorender.io/LOKVTtKVGb/r_15,w_600,h_600,c_crop,br_20/doc1/flight.jpg" alt="Multiple transformations" style={{ borderRadius: '0.5rem', maxWidth: '100%' }} />

