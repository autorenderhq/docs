---
title: 'Direct upload'
description: 'Upload a file with multipart/form-data using an API key.'
openapi: api-reference/openapi.json POST /api/v1/uploads
---

## Overview

`POST /api/v1/uploads` is the primary upload endpoint for backend services.

## Required request

### Headers

| Header | Value |
| --- | --- |
| `Authorization` | `Bearer YOUR_API_KEY` |
| `Content-Type` | `multipart/form-data` |

### Form fields

| Field | Required | Description |
| --- | --- | --- |
| `file` | Yes | Binary file payload |
| `file_name` | Yes | Final filename in your workspace |

## Minimal example

```bash
curl -X POST \
  https://upload.autorender.io/api/v1/uploads \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -F "file=@./image.jpg" \
  -F "file_name=image.jpg"
```

## Common optional fields

| Field | Description |
| --- | --- |
| `folder` | Organize destination path (`products/shoes`) |
| `tags` | Comma-separated labels for filtering |
| `transform` | Apply transformation during upload |
| `metadata` | JSON object string with custom fields |
| `custom_id` | Internal reference ID from your app |
| `random_prefix` | Add random suffix to reduce name collisions |

## Success response shape

```json
{
  "success": true,
  "data": {
    "file_no": "1234567890",
    "name": "image.jpg",
    "url": "https://assets.autorender.io/...",
    "path": "products/image.jpg",
    "format": "jpg",
    "file_size": 102400
  }
}
```

## Error pattern

```json
{
  "success": false,
  "error": "Unauthorized",
  "message": "Invalid or missing API key"
}
```
